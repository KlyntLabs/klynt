# Default configuration for Klynt backend

[api]
host = "127.0.0.1"
port = 3000
allowed_origins = ["http://localhost:5174"]

[rate_limiter]
enabled = false
max_requests = 100
window_seconds = 60

log_level = "debug"
hsts_enabled = false
cookie_domain = ".klynt.edu"
cookie_secure = false
