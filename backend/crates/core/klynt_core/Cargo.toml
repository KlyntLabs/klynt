[package]
name = "klynt_core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Time
chrono = { workspace = true }

# IDs
uuid = { workspace = true }

# Internal crates
klynt_utils = { path = "../../shared/klynt_utils" }
