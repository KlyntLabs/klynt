# Install: lefthook install
# Docs: https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  commands:
    rustfmt:
      glob: "*.rs"
      run: cd backend && cargo fmt --all -- --check
    biome-check:
      glob: "*.{js,ts,tsx,json,jsonc,css}"
      run: cd frontend && npx @biomejs/biome check --no-errors-on-unmatched {staged_files}

pre-push:
  commands:
    clippy:
      glob: "*.rs"
      run: cd backend && cargo clippy --all-targets --all-features -- -D warnings
    rust-tests:
      glob: "*.rs"
      run: cd backend && cargo test
    frontend-check:
      glob: "*.{js,ts,tsx}"
      run: cd frontend && npm run check
